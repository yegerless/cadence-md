apiVersion: v1
kind: Service
metadata:
  name: vllm-embeddings
  labels:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: vllm-embeddings
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: vllm-embeddings
