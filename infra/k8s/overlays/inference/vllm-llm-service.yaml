apiVersion: v1
kind: Service
metadata:
  name: vllm-llm
  labels:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: vllm-llm
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: vllm-llm
