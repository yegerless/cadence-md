apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: backend
spec:
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: backend
