apiVersion: v1
kind: Service
metadata:
  name: rag-worker
  labels:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: rag-worker
spec:
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
  selector:
    app.kubernetes.io/name: cadence-md
    app.kubernetes.io/component: rag-worker
